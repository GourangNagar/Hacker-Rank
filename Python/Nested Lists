if __name__ == '__main__':
    n = int ( input () )
    a = [ [ input(), float ( input() ) ] for i in range ( n ) ] # input
    s = sorted ( set ( [  x [1] for x in a ] ) )  # s is sorted a on bases of numbers
    for name in sorted ( x [0] for x in a if x [1] == s[1] ) :
        print (name)
